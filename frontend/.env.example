# Frontend build-time config (Vite). Copy to .env.local for local overrides.
#
# In production the SPA is served from the same origin as the Spring backend,
# so the API base is a relative "/api" and needs no configuration.
# Set this only if you host the frontend on a different origin than the API.
VITE_API_BASE=/api
